{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Phonebook app for part 3 of fullstackopen course",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force -ErrorAction SilentlyContinue dist && cd ../phonebook-fe && npm run build && @powershell Copy-Item dist -Recurse ../phonebook",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "mongoose": "^8.13.2",
    "morgan": "^1.10.0",
    "sentry": "^0.1.2"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
